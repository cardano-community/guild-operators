{
  "function": "totals(_epoch_no)",
  "description": "Get the treasury, total active supply in lovelace for specified epoch, all epochs if empty",
  "parameters": [
    {
      "name": "_epoch_no",
      "description": "[optional] the epoch number to get active supply for. All epoch if omitted."
    }
  ],
  "example": [
    {
      "type": "rest",
      "command": "curl -d _epoch_no=122 -s http://localhost:8050/rpc/totals"
    }
  ]
}
