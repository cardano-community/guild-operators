{
  "function": "pool_active_stake(_pool_bech32, _epoch_no)",
  "description": "Get the pools active stake in lovelace for specified epoch, current epoch if empty",
  "parameters": [
    {
      "name": "_pool_bech32",
      "description": "pool id in bech32 format. Total stake for epoch if omitted."
    },
    {
      "name": "_epoch_no",
      "description": "[optional] the epoch number to get active stake for. Current epoch if omitted."
    }
  ],
  "example": [
    {
      "type": "rest",
      "command": "curl -d _pool_bech32=pool1z2ry6kxywgvdxv26g06mdywynvs7jj3uemnxv273mr5esukljsr -d _epoch_no=122 -s http://localhost:8050/rpc/pool_active_stake"
    }
  ]
}
