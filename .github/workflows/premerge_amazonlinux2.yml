name: Pre-merge AmazonLinux2 Prerequisites

on:
  workflow_dispatch:
  pull_request:
    paths:
      - scripts/cnode-helper-scripts/prereqs.sh
      - scripts/cnode-helper-scripts/cabal-build-all.sh
      - files/cabal.project.local

jobs:
  prerequisites:
    runs-on: ubuntu-latest
    steps:
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - uses: actions/checkout@v2
    - name: Extract branch from PR
      shell: bash
      run: echo "##[set-output name=branch;]${GITHUB_HEAD_REF}"
      id: pr_branch
    - name: Testing prereqs.sh
      run: |
        export BRANCH=${{ steps.pr_branch.outputs.branch }}
        docker build . --file files/tests/pre-merge/amazonlinux2_prereqs.sh.containerfile --compress --build-arg BRANCH --tag ghcr.io/cardano-community/pre-merge:amazonlinux2_prereqs
    - name: Push pre-merge:amazonlinux2_prereqs
      run: |
        docker push ghcr.io/cardano-community/pre-merge:amazonlinux2_prereqs

  build:
    needs: prerequisites
    runs-on: ubuntu-latest
    steps:
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - uses: actions/checkout@v2
    - name: Extract branch from PR
      shell: bash
      run: echo "##[set-output name=branch;]${GITHUB_HEAD_REF}"
      id: pr_branch
    - name: Testing cabal-build-all.sh
      run: |
        export BRANCH=${{ steps.pr_branch.outputs.branch }}
        docker build . --file files/tests/pre-merge/amazonlinux2-cabal.containerfile --compress --build-arg BRANCH --tag ghcr.io/cardano-community/pre-merge:amazonlinux2_cabal

  prerequisites-with-system-libsodium:
    runs-on: ubuntu-latest
    steps:
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - uses: actions/checkout@v2
    - name: Extract branch from PR
      shell: bash
      run: echo "##[set-output name=branch;]${GITHUB_HEAD_REF}"
      id: pr_branch
    - name: Testing prereqs.sh -l
      run: |
        export BRANCH=${{ steps.pr_branch.outputs.branch }}
        docker build . --file files/tests/pre-merge/amazonlinux2_prereqs.sh_-l.containerfile --compress --build-arg BRANCH --tag ghcr.io/cardano-community/pre-merge:amazonlinux2_prereqs_-l
    - name: Push pre-merge:amazonlinux2_prereqs_-l
      run: |
        docker push ghcr.io/cardano-community/pre-merge:amazonlinux2_prereqs_-l

  build-all-with-system-libsodium:
    needs: prerequisites-with-system-libsodium
    runs-on: ubuntu-latest
    steps:
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - uses: actions/checkout@v2
    - name: Extract branch from PR
      shell: bash
      run: echo "##[set-output name=branch;]${GITHUB_HEAD_REF}"
      id: pr_branch
    - name: Testing cabal-build-all.sh -l
      run: |
        export BRANCH=${{ steps.pr_branch.outputs.branch }}
        docker build . --file files/tests/pre-merge/amazonlinux2-cabal_-l.containerfile --compress --build-arg BRANCH --tag ghcr.io/cardano-community/pre-merge:amazonlinux2_cabal_-l
